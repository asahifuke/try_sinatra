<button style="margin-right: 5px;" onclick='location.href="/memos/new"'>追加</button>
<% @memos.each do |memo| %>
  <div>
    <a href= "/memos/<%= memo.id %>">
      <%= memo.title %>
    </a>
  </div>
<% end %>
